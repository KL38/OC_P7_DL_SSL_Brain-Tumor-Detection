# --- Data & heavy artefacts ---------------------------------------------------
# Images, embeddings and weights are all regenerated by running the notebooks in
# order. None of them belong in a Git repository.
data/
embeddings/
*.pth
*.zip

# Pseudo-labels and test labels produced by the clustering notebooks.
pseudo_labels_gmm*.csv
true_labels_test.csv
comparison_results.csv
history_*.csv

# --- Course material & OpenClassrooms deliverables ----------------------------
# Kept locally, outside the repository: submission bundles, slide decks and
# superseded drafts. None of it is code, and it duplicates `notebooks/`.
livrables/
Cours/
Notebook olds/
*.pptx

# --- Tooling ------------------------------------------------------------------
.claude/
.ruff_cache/
.mypy_cache/
.pytest_cache/
.venv/
__pycache__/
*.pyc

# --- Jupyter ------------------------------------------------------------------
.ipynb_checkpoints/
